# UX preview mode for Vercel or frontend-only demos.
# true  = use in-browser mock data, no server/Docker/PostgreSQL needed.
# false = call NEXT_PUBLIC_API_URL.
NEXT_PUBLIC_USE_MOCK=true

# Only used when NEXT_PUBLIC_USE_MOCK=false.
NEXT_PUBLIC_API_URL=http://localhost:4000
